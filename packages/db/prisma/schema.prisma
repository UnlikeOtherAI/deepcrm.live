// Minimal placeholder so `prisma generate` succeeds before T03 replaces
// this file with the full schema from docs/schema-engine.md §2.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
